function soma ( n1, n2) { // se eu colocar soma(n1=0, n2=0), ambos vão receber inicialmente o valor zero, e assim não da erro caso coloque só um numero.
    return n1 + n2 
}
console.log(soma(2, 3)) // o dois vai para o n1 e o tres vai n2